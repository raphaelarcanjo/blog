<%- contentFor('title') %>
Home

<%- contentFor('header') %>
<% if (typeof mensagem !== 'undefined') { %>
    <div class="card-panel green lighten-4 green-text">
        <%= mensagem %>
    </div>
<% } %>

<% if (typeof erro !== 'undefined') { %>
    <div class="card-panel red lighten-4 red-text">
        <%= erro %>
    </div>
<% } %>


<%- contentFor('body') %>
<h1>Bem-vindo(a) ao <span class="teal-text">Seu Blog</span></h1>

<%- contentFor('footer') %>
